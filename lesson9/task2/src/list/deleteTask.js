export const onDeleteTask = (taskId) => {
  const listItemElem = document.querySelectorAll(".list-item");

  // if (deletedData) {
  //   listItemElem.remove();
  // }

  //   console.log("Clicked");
};
